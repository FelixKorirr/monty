#include "monty.h"

/**
 * stack_func - prints the top
 * @head: head pointer
 * @x: line_no
 * Return: nothing
 * korir codes
*/

void stack_func(stack_t **head, unsigned int x)
{
	(void)head;
	(void)x;
	newnode.lifi = 0;
}
